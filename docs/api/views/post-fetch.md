# PostFetch

Helper for attaching related rows to a list of base objects after the
base query has run. See the [PostFetch guide](../../guide/views/post-fetch.md)
for the join model, performance characteristics, and view integration.

::: restflow.views.PostFetch
