# InvalidationRule

The dataclass that connects a Django model's signals to a
`CachedWrapper`. See the
[Invalidation Rules guide](../../guide/caching/invalidation.md)
for an overview and worked examples.

::: restflow.caching.InvalidationRule
    options:
      members:
        - resolve_invalidator
        - get_dispatcher
