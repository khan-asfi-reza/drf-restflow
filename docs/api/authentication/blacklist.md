# Blacklist backends

Reference for the abstract blacklist base class and the two
concrete backends shipped with Restflow. See the
[Blacklist section of the JWT guide](../../guide/authentication/jwt.md#blacklist-backends)
for selection and configuration notes.

::: restflow.authentication.BlacklistBackend

::: restflow.authentication.CacheBlacklistBackend

::: restflow.authentication.ModelBlacklistBackend
