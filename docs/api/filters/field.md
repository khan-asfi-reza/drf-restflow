# Field

The base filter field. Every concrete field type extends `Field` and
inherits the same set of common parameters. See the
[Fields guide](../../guide/filtering/fields.md) for an overview and
worked examples.

::: restflow.filters.Field
    options:
      members:
        - apply_filter
        - clone
        - get_method
