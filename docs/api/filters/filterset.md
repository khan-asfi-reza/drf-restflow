# FilterSet

The class that validates query parameters and applies filters to a
Django queryset. See the
[FilterSet guide](../../guide/filtering/filterset.md) for an
overview, Meta options, ordering, processors, validation, and
PostgreSQL features.

::: restflow.filters.FilterSet
    options:
      members:
        - model_dump
        - filter_queryset
        - get_options
