# InlineFilterSet

Factory that builds a `FilterSet` class from a plain dict of fields,
without writing a class explicitly. See the
[FilterSet guide](../../guide/filtering/filterset.md#inlinefilterset)
for an overview and worked examples.

::: restflow.filters.InlineFilterSet
